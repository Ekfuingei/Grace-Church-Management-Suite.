# Supabase (optional — omit both to use demo mode)
# When VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are empty or missing,
# the app runs in demo mode with built-in login (no database required).
#
# Demo: admin@demo.gracechurch.org | media@demo.gracechurch.org (password: demo123)
#
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
